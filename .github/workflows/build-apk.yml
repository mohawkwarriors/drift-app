name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python Image Library
        run: python3 -m pip install --upgrade Pillow

      - name: Prepare Web Assets
        run: |
          mkdir -p www
          cp -r index.html manifest.json site.webmanifest sw.js *.png favicon.ico www/ 2>/dev/null || true

      - name: Install Capacitor Dependencies
        run: |
          npm install @capacitor/cli @capacitor/core @capacitor/android

      - name: Add Android Platform
        run: |
          npx cap add android || true

      - name: Sync Capacitor
        run: |
          npx cap sync android

      - name: Configure Android Manifest (Standard Icon Only, No RoundIcon)
        run: |
          manifest_file="android/app/src/main/AndroidManifest.xml"
          if ! grep -q "android.permission.VIBRATE" "$manifest_file"; then
            sed -i '/<application/i \    <uses-permission android:name="android.permission.VIBRATE" />' "$manifest_file"
          fi
          # Ensure standard icon is set and remove roundIcon attribute completely
          sed -i 's/android:icon="[^"]*"/android:icon="@mipmap\/ic_launcher"/g' "$manifest_file"
          sed -i 's/android:roundIcon="[^"]*"//g' "$manifest_file"

      - name: Fix Kotlin Duplicate Classes Conflict
        run: |
          cat << 'EOF_GRADLE' >> android/build.gradle

          allprojects {
              configurations.all {
                  exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk7'
                  exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk8'
              }
          }
          EOF_GRADLE

          cat << 'EOF_APP_GRADLE' >> android/app/build.gradle

          configurations.all {
              exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk7'
              exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk8'
          }

          dependencies {
              modules {
                  module("org.jetbrains.kotlin:kotlin-stdlib-jdk7") {
                      replacedBy("org.jetbrains.kotlin:kotlin-stdlib", "kotlin-stdlib-jdk7 is now part of kotlin-stdlib")
                  }
                  module("org.jetbrains.kotlin:kotlin-stdlib-jdk8") {
                      replacedBy("org.jetbrains.kotlin:kotlin-stdlib", "kotlin-stdlib-jdk8 is now part of kotlin-stdlib")
                  }
              }
          }
          EOF_APP_GRADLE

      - name: Run Icon Generator
        run: |
          python3 generate_icons.py
          rm -rf android/app/src/main/res/mipmap-anydpi-v26 || true

      - name: Build Android App (APK)
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: drift-app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk

      - name: Publish APK to GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          apk_file=$(find android/app/build/outputs/apk/debug/ -name "*.apk" | head -n 1)
          if [ -f "$apk_file" ]; then
            cp "$apk_file" drift-app.apk
            gh release delete latest --yes 2>/dev/null || true
            gh release create latest drift-app.apk --title "Latest APK Build" --notes "Direct squircle icon build"
            echo "Successfully published drift-app.apk to latest release!"
          else
            echo "Error: APK file not found!"
            exit 1
          fi
